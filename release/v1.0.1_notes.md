# Release Notes - v1.0.1

**Release Date:** 2026-02-07

## 🚀 Performance Improvements

### Optimized Caching
- Implemented intelligent caching for extension settings and managed sites list
- Reduces redundant calls to `chrome.storage.sync`
- Improves response time for frequent actions like page loads and tab updates

### Parallel Cookie Deletion
- Refactored cookie clearing logic to handle deletions in parallel
- Significantly speeds up clearing for sites with many cookies
- Added deduplication to prevent redundant API calls

### UI Responsiveness
- Added 300ms debounce to the managed sites search input
- Prevents UI stuttering when filtering large lists of sites

## 🛠 Fixes & Reliability

- **Error Handling**: Added checks for `chrome.runtime.lastError` to prevent context menu creation errors
- **Graceful Fallbacks**: Settings retrieval now safely falls back to storage if cache is empty
- **Async Logic**: Fixed potential race conditions in message handling

## 📦 Installation

1. Download `site-storage-cleaner-v1.0.1.zip`
2. Extract the archive
3. Load unpacked extension in Chrome Developer Mode
